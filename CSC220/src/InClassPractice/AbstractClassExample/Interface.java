package InClassPractice.AbstractClassExample;

public interface Interface {

    double E = 2.78;
    
    // -- this abstract function can be defined in either the abstract class that implements it or the concrete class
    // that extends the abstract class
    public void interfaceFunction();
}
